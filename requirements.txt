# --- 골격 + 대시보드(현재 슬라이스)에 필요한 의존성 ---
python-dotenv      # .env 로딩
requests           # Hermes localhost HTTP 클라이언트
fastapi            # 대시보드 JSON API + Hermes 인바운드
uvicorn            # FastAPI ASGI 서버
apscheduler        # 폴링/브리핑 스케줄러 (후속 슬라이스)

# --- 데이터소스 (대시보드 시세/차트) ---
finance-datareader # 국내 시세 (005930, 000660)
yfinance           # 미국 MU/^SOX/NQ=F 시세
pandas             # 시세 데이터 처리

# --- 후속 슬라이스에서 추가 예정 (분석/마케팅) ---
# pykrx               # 국내 시세 보조
# OpenDartReader      # 국내 공시/재무
