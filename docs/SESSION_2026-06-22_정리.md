# 세션 정리 — 2026-06-22 (완료/미완료)

자동매매봇 고도화 + AI 리포트 + Hermes 텔레그램 비서 + 종목관리 + 보안.
모든 코드는 GitHub + VPS(`/root/stock-watchdog`, systemd) 동기화 완료. 비밀번호·키는 VPS `.env`에만.

## ✅ 완료 (라이브 배포됨)

### 자동매매봇 탭 (🔒 비밀번호 1220)
- 설정 → 자동매매봇 탭 분리, 클라이언트 게이트 + **서버 인증**(`/api/order`, `/api/autotrade/config`가 `TRADE_PASSWORD` 검증, fail-closed=미설정 시 주문 전면 거부)
- **주문 테스트**(수동 1주 지정가, **종목 드롭다운 직접 선택** — 하드코딩 제거) · **보유 종목**(잔고/평단/손익) · **체결/미체결 내역**(최근 7일)
- **자동매매**: 등락률 밴드(매수%·매도%) + 손절(손절가·평단대비%) + 예약매도(HH:MM). 종목·행위당 하루 1회, qty 상한, master OFF 기본
- **🦶 발목(저점권) 감지**: RSI·52주·볼린저·스토캐스틱·피보 합류(min_score). 알람(카톡/텔레그램) + 옵션 자동매수 + **AI 판단(더듬이2·3) 옵션**(매수신호일 때만 매수) + 지금스캔

### 더듬이 (탭 순서: 1 → 4 → 2·3)
- **더듬이4**: KODEX200 → **5개 테마**(조선·방산·원전·반도체·소부장) 대표종목 25개. **신규편입 + 급증매수**(당일≥직전4일평균×2) 포착 + **AI 한줄 코멘트**(Haiku)
- 더듬이2·3 **진화기록**(신호 변천 + 적중률) 종목 선택 시 무료 표시

### AI 리포트 3종 (자동 발송 + 지금생성 버튼)
- 📊 **장 마감 리포트**: 평일 15:40 — 시세·테마·발목·보유 → Claude 요약+내일포인트
- 💼 **보유종목 데일리 케어**: 평일 09:30 — 홀딩/손절/익절 추천(추천만)
- 🗓️ **주간 복기**: 토 09:00 — 매매내역·적중률·보유 복기
- 전부 카톡·텔레그램 발송. 모델 **Haiku**(저렴)

### 기타
- 💰 **토큰 사용량 측정**(설정) — 앱 Claude 비용 표시
- 📋 **종목 관리 UI**(설정) — 6자리 코드로 동적 추가/삭제, **재시작 불필요**(`watchlist.json`)
- 🔒 **보안**: 비번 `1205` 코드에서 제거→`.env TRADE_PASSWORD=1220`. `.env` 백업 PC(`Documents\stock-env-backup`)
- ⚙️ **VPS 자동배포**: 이 PC SSH키로 `git pull + restart`(`ssh -i ~/.ssh/id_ed25519 root@187.127.118.211`)
- 🐛 **fix**: 잔고/체결내역 KIS 500(유량초과) 재시도(`_get_retry`); list_orders 토큰오타

### 🤖 Hermes 텔레그램 비서 (새 시도)
- Hermes = AI 에이전트(v0.16.0, sonnet). **텔레그램으로 말 걸면 우리 앱 API를 `curl`로 호출해 답**
- `SOUL.md`에 "주식 비서 역할 + 앱 API 목록" 추가, 텔레그램 플랫폼에 `terminal`·`web` 도구 enable(`hermes tools enable --platform telegram terminal web`) + 게이트웨이 재시작
- **개념**: 앱=엔진(데이터), Hermes=비서(말로 다루기). Hermes 자체는 "두뇌"가 아니라 Claude를 부르는 매니저
- **최종 결론(사용자와 합의)**: 일반 질문은 그냥 **Claude(앱/claude.ai)** 직접이 정답. 사적 데이터(내 계좌)·자동·실행은 **우리 앱이 이미 다 함**. → **Hermes는 텔레그램 배달부로 그냥 둠**(무리한 기능화 중단). Hermes 고유 영역은 **computer-use(API 없는 사이트 클릭 조작)** 뿐이며 미착수(필요해지면 그때).

## ⬜ 미완료 / 다음 세션

- **월요일 장중 실거래 검증**: 1주 매수→매도 1사이클, 자동매매/발목/리포트 실동작 확인 (지금 예수금 7원 — 입금 필요)
- **만원 미만 종목 추가**해서 싸게 1주 테스트 (종목코드 정하면 종목관리에서 추가)
- **Hermes 비서 재테스트**: terminal 도구 켠 뒤 텔레그램으로 "발목 종목 알려줘" 등 실제 답 오는지
- **손절/밴드 체결 확실성**: 지정가=현재가 → 급락 시 미체결 가능 → "현재가보다 약간 낮게" 옵션(미적용)
- **발목/밴드 하루 1회 → 여러 번** 확장 옵션(미적용)
- **computer-use**(API 없는 사이트 조작) — Hermes 진짜 고유 영역, 미착수(VPS 데스크톱 환경 필요)
- 보안: `:8000` 외부 직접접속 차단(미작업)
- 더듬이1 알림 "준비 중" 라벨 정리

## 핵심 사실 (헷갈렸던 것)
- **Claude API = 두뇌(돈 나감)** / **Hermes = 텔레그램 배달부(+비서 매니저)** / 카카오는 앱이 직접 발송
- AI 토큰 쓰는 곳: 더듬이2·3, 브리핑, 찰떡써머리, 리포트 3종, 더듬이4 코멘트, 발목 AI판단. **자동매매·발목감지·더듬이1/4 계산은 토큰 0**
- 코드 변경은 **PC→GitHub→VPS** 3곳. VPS는 24시간 systemd. `.env`는 VPS만.
