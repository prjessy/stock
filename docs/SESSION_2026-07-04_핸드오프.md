# 세션 핸드오프 2026-07-04 — 종목명 검색 · PWA jessystock · 로그인 속도

> 3건 모두 **VPS 라이브 배포 완료** (표준 scp + `systemctl restart stock-watchdog`, git 미경유).
> 백업: VPS `~/stock-watchdog/.deploy_bak/` — `20260704_120556`(국내검색) · `122329`(미국검색) · `124120`(PWA) · `132113`(속도)

---

## ✅ 완료 1 — 종목 추가를 「종목명 자동완성」으로 (VOC: 코드 못 외움)

- 대시보드 「＋ 내 종목 추가」·⚙️설정 종목 관리 입력창에 **이름만 치면 드롭다운** → 클릭 즉시 추가.
  - 국내: `삼성전` → 삼성전기/삼성전자/… (KRX 전종목 2,873개)
  - 미국: 한글 별칭 `애플`·`테슬라` 등 ~70개 + 영문명 `apple` (6,700티커)
  - 이름 정확일치면 Enter만 쳐도 서버가 코드로 해석(`_resolve_symbol_by_name`). 기존 코드/티커 입력도 그대로.
- 신규 API: `GET /api/symbol-search?q=` (500 금지, 국내+미국 병합)
- **VPS의 KRX 차단 대비**: 스냅샷 폴백 — `app/datasources/krx_names.json`(국내), `us_names.json`(미국). FDR 실패 시 자동 사용.
- 변경: `api.py`, `kr_price.py`(+`search_names`), `us_market.py`(+`US_KR_ALIASES`/`search_names`), `index.html`(`attachSymbolSuggest`/`.sym-sug`), 스냅샷 json 2개
- 검증: VPS에서 `삼성전`/`애플`/`apple` 검색 정상 응답 확인.
- 상세: `docs/SESSION_2026-07-04_종목명검색추가.md`

## ✅ 완료 2 — PWA 이름 jessystock 으로

- PWA 자체는 6/26에 이미 완성(manifest+sw.js+아이콘) → **이름만 변경**: manifest `name/short_name`, `<title>`, `apple-mobile-web-app-title` 전부 `jessystock`.
- 검증: `/manifest.webmanifest` 200 + 새 이름, `/sw.js` 200, `<title>jessystock</title>` 확인.
- ⚠️ 사용자: 기존 홈화면 아이콘은 **삭제 후 재추가**해야 새 이름 반영.

## ✅ 완료 3 — 로그인까지 3~5초 지연 (VOC)

원인 2개, 둘 다 수정:
1. **프론트 순차 로딩**: `init()`이 시세→차트→재무를 순차 await 후에야 `loadAuth()` → 로그인 버튼 지각.
   → `loadAuth()` **맨 먼저+병렬**, history/intraday/fundamentals 병렬화 (`index.html` 맨 끝 init).
2. **KIS 접속 불가**(증권사 업그레이드 점검 — 사용자 확인): 재무 조회가 매번 8초 타임아웃 소진.
   → `kis_price.py`: `_TIMEOUT=(3,7)` 연결/읽기 분리, **서킷브레이커** `_net_blocked()/_note_net_fail()`(연결계열 예외 시 30초 KIS 전체 쉼; quote/fundamentals/investor/orderbook 적용), 재무 60초 캐시(실패 포함).
- 실측: 재무 8.3초→0.001초, 주요 응답 전부 0.3~0.7초. 사용자 "빨라짐" 확인.
- 남는 ~1초는 해외 VPS 왕복(구조적). 더 줄이려면 국내 VPS 이전이 유일한 큰 레버.

---

## ⬜ 미완료 / 다음 세션

1. **git 커밋** — 오늘 변경분 전부 워킹트리에만 있음(기존 방침: 사용자 지시 시 커밋). 커밋 시 `app/llm.py`·`.env`·`key*.txt` 제외 필수.
2. **KIS 점검 복구 확인** — 증권사 업그레이드 종료 후(월요일 장중) 재무/수급 자동 복구 확인. 서킷브레이커가 30초 주기 재시도라 조치 불필요, 확인만. 여전히 "KIS 연결 불가"면 해외IP 차단 조사.
3. **종목명 스냅샷 갱신 주기** — 신규 상장종목은 스냅샷에 없음. 필요 시 FDR로 재생성 후 scp (절차: `docs/SESSION_2026-07-04_종목명검색추가.md`).
4. **미국 한글 별칭 확장** — 현재 인기종목 ~70개. 요청 오면 `us_market.py`의 `US_KR_ALIASES`에 추가.
5. **플레이스토어 등록**(대기) — PWA 완료 상태. 다음 단계 PWABuilder→TWA→Play Console ($25 1회). 절차: `docs/TODO_PWA_아이폰세션_플레이스토어.md`.

## 운영 메모
- 배포 = scp + restart (git pull 아님). SSH/경로는 로컬 메모리 `deploy-method-scp`·`stock-watchdog-deploy` 참조(공개 레포에 IP·키 금지).
- 브라우저 캐시: 변경 반영 안 보이면 Ctrl+Shift+R.
- 관련 메모리: `watchlist-name-search`, `login-slow-fix`.
